# パスワード設定ガイド

## 現在のパスワード

デフォルトパスワード: `oma2024`

## パスワードの変更方法

### ステップ1: 新しいパスワードを決める

受講者に配布する新しいパスワードを決定します。

例: `oma-dao-2025` や `metagri2024` など

### ステップ2: SHA-256ハッシュを生成

以下のいずれかの方法でSHA-256ハッシュを生成します：

#### 方法A: オンラインツールを使用（推奨）

1. https://emn178.github.io/online-tools/sha256.html にアクセス
2. 新しいパスワードを入力
3. 生成されたハッシュをコピー

#### 方法B: コマンドラインを使用

```bash
# Macの場合
echo -n "あなたのパスワード" | shasum -a 256

# Linuxの場合
echo -n "あなたのパスワード" | sha256sum
```

### ステップ3: index.htmlを更新

`index.html` ファイルを開き、以下の行を探します：

```javascript
const PASSWORD_HASH = '8d969eef6ecad3c29a3a629280e686cf0c3f5d5a86aff3ca12020c923adc6c92';
```

この値を、ステップ2で生成したハッシュに置き換えます：

```javascript
const PASSWORD_HASH = 'あなたの新しいハッシュ';
```

### ステップ4: 変更をコミット＆プッシュ

```bash
git add index.html
git commit -m "Update password hash"
git push
```

## セキュリティに関する注意

### このパスワード保護の仕組み

- **クライアントサイド認証**: ブラウザ上でパスワードを検証
- **SHA-256ハッシュ**: パスワードをハッシュ化して保存
- **セッションストレージ**: ログイン状態を一時的に保持

### セキュリティレベル

- ✅ **十分**: 受講者向けの限定公開
- ✅ **十分**: 内部資料の共有
- ❌ **不十分**: 機密情報の保護
- ❌ **不十分**: 高度なセキュリティが必要な用途

### より高いセキュリティが必要な場合

より高度なセキュリティが必要な場合は、以下を検討してください：

1. **サーバーサイド認証**: Next.js + Vercel Password Protection
2. **OAuth認証**: Google/GitHub アカウントでのログイン
3. **アクセス制限**: IPアドレスベースのアクセス制限

## パスワード配布のベストプラクティス

1. **安全な配布方法**
   - Discord DMで個別送信
   - 講義の最初に口頭で伝える
   - 印刷資料として配布

2. **避けるべき配布方法**
   - 公開SNSでの共有
   - 不特定多数が閲覧可能な場所への投稿

## トラブルシューティング

### パスワードが合わない

1. ハッシュ生成時に余分なスペースや改行が含まれていないか確認
2. `echo -n` を使用しているか確認（`-n`がないと改行が含まれる）
3. パスワードの大文字・小文字が正確か確認

### ログイン状態が保持されない

- セッションストレージを使用しているため、ブラウザを閉じるとログアウトされます
- これは正常な動作です

### GitHub Pagesで更新が反映されない

1. 数分待ってから再度アクセス
2. ブラウザのキャッシュをクリア（Shift + F5）
3. GitHub Actionsのログを確認

## 参考リンク

- SHA-256オンラインツール: https://emn178.github.io/online-tools/sha256.html
- MDN Web Crypto API: https://developer.mozilla.org/en-US/docs/Web/API/SubtleCrypto/digest
